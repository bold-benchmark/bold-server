# Configuration of the simulation engine

# initialization
# bold.init.dataset = data/*.trig

# runtime configuration
# bold.runtime.query = query/sim-*.rq

# replay configuration
# %d: iteration number
bold.replay.dump = dump/dataset-%d.trig

# transition system configuration
# bold.sparql.init = query/init-sim-*.rq
# bold.sparql.update = query/update-sim-*.rq
bold.fmu.filename = Accelerate.fmu

# Server configuration
# default: 8080
#bold.server.httpPort = 8080